{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

    <div class="container mt-5">
        <h1 class="text-center">Confirmation de votre commande</h1>
        
        <p>
            {##### prénom du client récupéré dynamiquement. #####}
            {# Bonjour {{ client.prenom }}, #}
        </p>
        <p>
            Merci pour votre commande sur Village Green !
            Nous avons bien reçu votre commande passée le

            {##### date de la commande jour/mois/année  #####}
            {# {{ commande.date|date('d/m/Y') }},  #}

            et elle est actuellement en cours de traitement.
        </p>

        <h3>Détails de votre commande :</h3>

        <ul>
            {#### boucle qui affiche les produits, quantité, prix formaté en euro #####}
            {# {% for produit in commande.produits %}
                <li>{{ produit.nom }},#} 
                Quantité : 
                {#{{ produit.quantite }} -#} 
                Prix :
                {#} {{ produit.prix|number_format(2, ',', ' ') }} €</li>
            {% endfor %} #}
        </ul>

        <p>
           
            <strong>Montant total :</strong>

            {##### montant total de la commande formaté en euro #####}
            {# {{ commande.montant_total|number_format(2, ',', ' ') }} € #}<br><br>
            <strong>Adresse de livraison :</strong>
            {#{{ commande.adresse }#}
        </p>

        <p>
            Vous recevrez un email de confirmation de votre commande sous peu.
        </p>

        <div class='justify-content-end'> 
            <p>
                Merci et à bientôt !
            </p>
        </div>
       
    </div>

{% endblock %}
